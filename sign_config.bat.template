SET CERT_FILENAME=pfx private certificate
SET CERT_PASSWORD=pfx password
SET CERT_CROSS_CERT=MS Cross Signing Certificate (if you have a proper authenticode cert)
SET CERT_PUBLIC_FILENAME=cer public certificate (if you _don't_ have a proper authenticode cert)
